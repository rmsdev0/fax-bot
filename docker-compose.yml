services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: faxbot
      POSTGRES_PASSWORD: faxbot
      POSTGRES_DB: faxbot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
